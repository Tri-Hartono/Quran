import Header from "@/components/Header";
import "./globals.css";
import Footer from "@/components/Footer";
import { Roboto } from "next/font/google";
import Providers from "./providers";
import ScrolltoTop from "@/components/ScrolltoTop";
import Logo from "./../public/logo.png";
import localFont from "next/font/local";

const Robboto = Roboto({
   weight: "400",
   subsets: ["latin"],
   display: "swap",
});

const myFont = localFont({ src: "../font/LPMQ IsepMisbah.ttf" });
export const metadata = {
   openGraph: {
      title: "Quran web Next.js",
      description: "Generated by create next app",
      url: "<https://trihartono-quran.vercel.app/>",
      siteName: "Next.js",
      images: [
         {
            url: "./../public/Logo.png",
            width: 256,
            height: 256,
         },
      ],
   },
};

export default function RootLayout({
   children,
}: {
   children: React.ReactNode;
}) {
   return (
      <html lang="en">
         <body className={`${myFont.className} px-setting relative"`}>
            <Providers>
               <div className="w-full 2xl:w-3/4 3xl:w-1/2 m-auto">
                  <Header />
                  {children}
                  <Footer />
                  <ScrolltoTop />
               </div>
            </Providers>
         </body>
      </html>
   );
}
